server:
  port: 2222

spring:
  application:
    name: GREET-API

  boot:
    admin:
      client:
        url: http://localhost:9091/

management:
  endpoints:
    web:
      exposure:
        include: '*'
